# Company X Environment Configuration
# Copy this to .env and adjust values as needed

# Database
DB_HOST=mysql
DB_NAME=talkmetrics
DB_USER=talkmetrics
DB_PASS=change_this_password

# MySQL root (for initial setup only)
MYSQL_ROOT_PASSWORD=change_this_root_password

# Redis
REDIS_HOST=redis
REDIS_PORT=6379